/**
 * Created by Vladyslav Lyfar on 05.10.2018.
 */
({
    showCreateCardForm: function(component, event, helper) {
        helper.showCreateCardForm(component);
    },
    createNewCard: function(component, event, helper) {
        helper.createNewCard(component);
    }
})